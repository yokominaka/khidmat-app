import { Platform } from 'react-native';

// Use 10.0.2.2 for Android Emulator, localhost for iOS simulator/web, 
// or replace with your machine's local IP address if running on a physical device.
export const API_BASE_URL = 'https://khidmat-app-8eze.onrender.com/api';
